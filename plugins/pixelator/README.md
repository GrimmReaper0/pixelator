# Pixelator Codex plugin

This repository-scoped plugin combines workflow skills with the local Pixelator MCP server. Open Codex at the repository root, install dependencies, and point `PIXELATOR_WORKSPACE` at the game being edited. Use skills for end-to-end decisions and MCP tools for guarded project actions; do not bypass revision checks with blind broad edits.
